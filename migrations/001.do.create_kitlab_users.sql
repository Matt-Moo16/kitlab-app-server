CREATE TABLE kitlab_users (
    user_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    username VARCHAR(100) NOT NUll UNIQUE,
    password TEXT NOT NULL, 
    date_created TIMESTAMP DEFAULT now() NOT NULL
);
